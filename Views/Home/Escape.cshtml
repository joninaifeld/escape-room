@{
    ViewData["Title"] = "Escape";
}
<img src= "/images/index.webp">
<section class="escape">
    <h1>@ViewBag.titulo</h1>
    <p>@ViewBag.descripcion</p>

    <form method="post" Action='@Url.Action("Index", "Home")'>
        <button type="submit" name="ans" value="next">Volver a jugar</button>
    </form>
</section>